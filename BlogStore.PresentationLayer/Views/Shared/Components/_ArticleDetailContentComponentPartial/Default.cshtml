@model BlogStore.EntityLayer.Entities.Article

<div class="post-content-body mb-4 p-3 bg-white shadow-sm rounded">
    <p class="fs-5 lh-base text-secondary">@Model.Description</p>
</div>

<div class="d-flex align-items-center gap-2 p-3 bg-light rounded shadow-sm">
    <i class="bi bi-person-circle fs-4 text-primary"></i>
    <h5 class="mb-0 text-dark">Yazar: <span class="fw-semibold">@ViewBag.username</span></h5>
</div>
